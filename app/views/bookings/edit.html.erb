<h1>Editing Booking</h1>

<%= render 'form', booking: @booking %>

<%= link_to 'Room Service', guest_booking_room_services_path(@booking.guest_id, @booking.id) %> |
<%= link_to 'Back', guest_path(@booking.guest_id) %>
