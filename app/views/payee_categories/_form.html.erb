<%= bootstrap_form_for(@payee_category) do |form| %>
  <% if payee_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payee_category.errors.count, "error") %> prohibited this payee_category from being saved:</h2>

      <ul>
      <% payee_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :category_name, id: :payee_category_category_name %>
  </div>

  <div class="field">
    <%= form.text_field :category_description, id: :payee_category_category_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
