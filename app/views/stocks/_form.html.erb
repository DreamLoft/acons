<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :stock_item_id %>
    <%= form.text_field :stock_item_id, id: :stock_stock_item_id %>
  </div>

  <div class="field">
    <%= form.label :filling_date %>
    <%= form.date_select :filling_date, id: :stock_filling_date %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :stock_quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
