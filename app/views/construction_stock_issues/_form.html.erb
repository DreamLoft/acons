<%= bootstrap_form_for(@construction_stock_issue, local: true) do |form| %>
  <% if construction_stock_issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(construction_stock_issue.errors.count, "error") %> prohibited this construction_stock_issue from being saved:</h2>

      <ul>
      <% construction_stock_issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :construction_stock_id %>
    <%= form.select(:construction_stock_id ,options_from_collection_for_select(ConstructionStock.all, :id, :item_name), :include_blank => "Select Item", skip_label: true) %>
  </div>

  <div class="field">
    <%= form.label :quanity %>
    <%= form.number_field :quanity, id: :construction_stock_issue_quanity, class:'form-control', skip_label: true, required: true %>
  </div>

  <div class="field">
    <%= form.label :issue_date %>
    <%= form.date_select :issue_date, id: :construction_stock_issue_issue_date, class:'form-control', skip_label: true, required: true %>
  </div>

  <div class="field">
    <%= form.label :issued_to %>
    <%= form.text_field :issued_to, id: :construction_stock_issue_issued_to, class:'form-control', skip_label: true, required: true %>
  </div>
<br>
  <div class="actions">
    <%= form.submit 'Save', class:'btn btn-primary' %>
  </div>
<% end %>
