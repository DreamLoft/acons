<%= form_with(model: [@office_deal, @office_deal.apartment_payments.build], local: true) do |form| %>
  <% if apartment_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apartment_payment.errors.count, "error") %> prohibited this apartment_payment from being saved:</h2>

      <ul>
      <% apartment_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :amount_paid %>
    <%= form.number_field :amount_paid, id: :apartment_payment_amount_paid, class:'form-control' %>
  </div>
<br>
  <div class="field">
    <%= form.label :payment_date %>
    <%= form.date_select :payment_date, id: :apartment_payment_payment_date, class:'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Save', class:'btn btn-primary' %>
  </div>
<% end %>
